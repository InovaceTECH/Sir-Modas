"use client";

import { ChevronDown, CreditCard, LoaderCircle, Minus, Plus, Search, ShoppingBag, Trash2, UserRound } from "lucide-react";
import Link from "next/link";
import { useActionState, useMemo, useState, useTransition } from "react";
import { createQuickCustomer } from "@/features/customers/actions/customer-actions";
import { createSale, type SaleActionState } from "../actions/sale-actions";

const initialState: SaleActionState = { status: "idle" };
type CatalogItem = { variantId: string; productName: string; color: string; size: string; stock: number; price: string; category: string | null };
type Customer = { id: string; name: string; phone: string };
type CartItem = CatalogItem & { quantity: number };
type Method = "cash" | "pix" | "debit_card" | "credit_card" | "on_account";
type Payment = { key: string; method: Method; amount: string; installments: number };
const currency = new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" });
const sources = ["Loja física", "Instagram", "WhatsApp", "Indicação", "Cliente antiga", "Passou em frente à loja", "Outro"];

export function SaleForm({ catalog, customers }: Readonly<{ catalog: CatalogItem[]; customers: Customer[] }>) {
  const [state, action, pending] = useActionState(createSale, initialState);
  const [search, setSearch] = useState("");
  const [cart, setCart] = useState<CartItem[]>([]);
  const [details, setDetails] = useState(false);
  const [discount, setDiscount] = useState("0");
  const [source, setSource] = useState("Loja física");
  const [notes, setNotes] = useState("");
  const [customerId, setCustomerId] = useState("");
  const [dueDate, setDueDate] = useState(() => { const date = new Date(); date.setDate(date.getDate() + 30); return date.toISOString().slice(0, 10); });
  const [payments, setPayments] = useState<Payment[]>([{ key: "initial", method: "pix", amount: "0", installments: 1 }]);
  const [quickCustomerOpen, setQuickCustomerOpen] = useState(false);
  const [quickCustomerError, setQuickCustomerError] = useState("");
  const [quickCustomerPending, startQuickCustomerTransition] = useTransition();
  const visible = useMemo(() => { const term = search.trim().toLocaleLowerCase("pt-BR"); return (term ? catalog.filter((item) => `${item.productName} ${item.color} ${item.size} ${item.category ?? ""}`.toLocaleLowerCase("pt-BR").includes(term)) : catalog).slice(0, term ? 20 : 12); }, [catalog, search]);
  const subtotal = cart.reduce((sum, item) => sum + Number(item.price) * item.quantity, 0);
  const total = Math.max(0, subtotal - (Number(discount) || 0));
  const paymentPayload = payments.length === 1 ? [{ method: payments[0].method, amount: total, installments: payments[0].installments }] : payments.map(({ method, amount, installments }) => ({ method, amount: Number(amount), installments }));
  const paid = paymentPayload.reduce((sum, payment) => sum + payment.amount, 0);
  const hasOnAccount = payments.some((payment) => payment.method === "on_account");
  const complete = Math.abs(paid - total) < 0.005;
  const add = (item: CatalogItem) => { if (item.stock < 1) return; setCart((current) => { const existing = current.find((entry) => entry.variantId === item.variantId); return existing ? current.map((entry) => entry.variantId === item.variantId ? { ...entry, quantity: Math.min(entry.quantity + 1, entry.stock) } : entry) : [...current, { ...item, quantity: 1 }]; }); };
  const quantity = (id: string, value: number) => setCart((current) => current.map((item) => item.variantId === id ? { ...item, quantity: Math.max(1, Math.min(value, item.stock)) } : item));
  const updatePayment = (key: string, field: keyof Omit<Payment, "key">, value: string) => setPayments((current) => current.map((payment) => payment.key === key ? { ...payment, [field]: field === "installments" ? Number(value) : value } : payment));
  const setMainMethod = (method: Method) => {
    setPayments((current) => current.map((payment, index) => index === 0 ? { ...payment, method } : payment));
    if (method === "on_account") setDetails(true);
  };
  const submitQuickCustomer = (formData: FormData) => { setQuickCustomerError(""); startQuickCustomerTransition(async () => { const result = await createQuickCustomer(formData); if (result.ok) { setCustomerId(result.customer.id); setQuickCustomerOpen(false); setDetails(true); } else setQuickCustomerError(result.message); }); };
  return <>
  <form action={action} className="grid gap-6 xl:grid-cols-[1.25fr_0.85fr]">
    <input type="hidden" name="items" value={JSON.stringify(cart.map(({ variantId, quantity }) => ({ variantId, quantity })))} /><input type="hidden" name="payments" value={JSON.stringify(paymentPayload)} /><input type="hidden" name="customerId" value={customerId} /><input type="hidden" name="dueDate" value={hasOnAccount ? dueDate : ""} /><input type="hidden" name="source" value={source} /><input type="hidden" name="discountAmount" value={discount} /><input type="hidden" name="notes" value={notes} />
    <div className="space-y-6"><section className="ui-card p-5"><div className="relative"><Search className="pointer-events-none absolute left-3.5 top-1/2 -translate-y-1/2 text-muted" size={18} /><input value={search} onChange={(event) => setSearch(event.target.value)} className="ui-input pl-11" placeholder="Buscar produto, tamanho ou cor" aria-label="Buscar produto" /></div><div className="mt-4 grid gap-2 sm:grid-cols-2">{visible.map((item) => <button key={item.variantId} type="button" disabled={item.stock < 1} onClick={() => add(item)} className="flex items-center justify-between gap-3 rounded-lg border border-border p-3 text-left transition hover:border-brand hover:bg-[#fff9f8] disabled:cursor-not-allowed disabled:opacity-45"><div className="min-w-0"><p className="truncate text-sm font-semibold">{item.productName}</p><p className="mt-0.5 text-xs text-muted">{item.color} · {item.size} · saldo {item.stock}</p></div><span className="shrink-0 text-sm font-semibold">{currency.format(Number(item.price))}</span></button>)}</div>{!catalog.length ? <p className="py-8 text-center text-sm text-muted">Cadastre produtos com estoque antes de vender.</p> : null}</section><section className="ui-card overflow-hidden"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><h2 className="font-semibold">Itens da venda</h2><p className="text-xs text-muted">{cart.reduce((sum, item) => sum + item.quantity, 0)} peça(s)</p></div><ShoppingBag size={19} className="text-brand-deep" /></div>{cart.length ? <ul className="divide-y divide-border">{cart.map((item) => <li key={item.variantId} className="grid gap-3 px-5 py-4 sm:grid-cols-[1fr_auto_auto] sm:items-center"><div><p className="font-medium">{item.productName}</p><p className="text-xs text-muted">{item.color} · {item.size} · {currency.format(Number(item.price))}</p></div><div className="flex items-center rounded-lg border border-border"><button type="button" onClick={() => quantity(item.variantId, item.quantity - 1)} className="grid size-9 place-items-center" aria-label="Diminuir quantidade"><Minus size={15} /></button><input value={item.quantity} onChange={(event) => quantity(item.variantId, Number(event.target.value))} type="number" min="1" max={item.stock} className="w-11 border-x border-border py-2 text-center text-sm outline-none" /><button type="button" onClick={() => quantity(item.variantId, item.quantity + 1)} className="grid size-9 place-items-center" aria-label="Aumentar quantidade"><Plus size={15} /></button></div><div className="flex items-center justify-end gap-2"><span className="min-w-24 text-right text-sm font-semibold">{currency.format(Number(item.price) * item.quantity)}</span><button type="button" onClick={() => setCart((current) => current.filter((entry) => entry.variantId !== item.variantId))} className="grid size-9 place-items-center rounded-lg text-muted hover:bg-red-50 hover:text-red-700" aria-label={`Remover ${item.productName}`}><Trash2 size={16} /></button></div></li>)}</ul> : <div className="px-5 py-12 text-center"><ShoppingBag className="mx-auto text-muted-soft" size={28} /><p className="mt-3 text-sm text-muted">Selecione os produtos acima.</p></div>}</section></div>
    <div className="space-y-6"><section className="ui-card p-5"><h2 className="font-semibold">Pagamento</h2><p className="mt-1 text-sm text-muted">Escolha como a cliente pagou.</p><label className="mt-4 block"><span className="text-xs font-semibold text-muted">Forma de pagamento</span><select value={payments[0]?.method ?? "pix"} onChange={(event) => setMainMethod(event.target.value as Method)} className="ui-input mt-1.5"><option value="cash">Dinheiro</option><option value="pix">Pix</option><option value="debit_card">Débito</option><option value="credit_card">Crédito</option><option value="on_account">Fiado</option></select></label>{payments[0]?.method === "credit_card" && payments.length === 1 ? <label className="mt-4 block"><span className="text-xs font-semibold text-muted">Parcelas</span><select value={payments[0].installments} onChange={(event) => updatePayment(payments[0].key, "installments", event.target.value)} className="ui-input mt-1.5">{Array.from({ length: 12 }, (_, value) => <option key={value + 1} value={value + 1}>{value + 1}x</option>)}</select></label> : null}<div className="my-5 border-y border-border py-4"><div className="flex justify-between text-sm text-muted"><span>Subtotal</span><span>{currency.format(subtotal)}</span></div>{Number(discount) > 0 ? <div className="mt-2 flex justify-between text-sm text-muted"><span>Desconto</span><span>− {currency.format(Number(discount))}</span></div> : null}<div className="mt-4 flex justify-between text-lg font-bold"><span>Total</span><span>{currency.format(total)}</span></div></div><button type="button" onClick={() => setDetails((value) => !value)} className="flex w-full items-center justify-between rounded-lg border border-border px-4 py-3 text-left text-sm font-semibold text-brand-deep hover:bg-[#fff9f8]" aria-expanded={details}><span>Mais opções</span><ChevronDown size={18} className={details ? "rotate-180 transition-transform" : "transition-transform"} /></button><p className="mt-2 text-xs text-muted">Cliente, desconto, origem, observações ou pagamento misto.</p></section>
      {details ? <section className="ui-card p-5"><div className="flex items-center justify-between"><h2 className="font-semibold">Mais opções</h2><button type="button" onClick={() => setQuickCustomerOpen(true)} className="text-xs font-semibold text-brand-deep">+ Cadastrar cliente</button></div><label className="relative mt-4 block"><span className="mb-1.5 block text-xs font-semibold text-muted">Cliente</span><UserRound className="pointer-events-none absolute bottom-3.5 left-3.5 text-muted" size={17} /><select value={customerId} onChange={(event) => setCustomerId(event.target.value)} className="ui-input pl-10"><option value="">Venda sem cliente vinculada</option>{customers.map((customer) => <option key={customer.id} value={customer.id}>{customer.name} · {customer.phone}</option>)}</select></label>{hasOnAccount ? <div className="mt-4 rounded-xl border border-amber-200 bg-amber-50 p-4"><p className="text-sm font-semibold text-amber-900">Para vender no fiado</p><label className="mt-3 block"><span className="text-xs font-semibold text-amber-900">Vencimento</span><input type="date" value={dueDate} onChange={(event) => setDueDate(event.target.value)} required className="ui-input mt-1.5" /></label>{!customerId ? <p className="mt-2 text-xs text-red-700">Selecione a cliente acima.</p> : null}</div> : null}<div className="mt-5 grid gap-4"><label><span className="text-xs font-semibold text-muted">Origem da venda</span><select value={source} onChange={(event) => setSource(event.target.value)} className="ui-input mt-1.5">{sources.map((item) => <option key={item}>{item}</option>)}</select></label><label><span className="text-xs font-semibold text-muted">Desconto</span><input value={discount} onChange={(event) => setDiscount(event.target.value)} type="number" min="0" max={subtotal} step="0.01" className="ui-input mt-1.5" /></label><label><span className="text-xs font-semibold text-muted">Observações</span><textarea value={notes} onChange={(event) => setNotes(event.target.value)} className="ui-input mt-1.5 min-h-20 py-3" /></label></div><div className="mt-5 border-t border-border pt-5"><div className="flex items-center justify-between"><div><h3 className="text-sm font-semibold">Pagamento misto</h3><p className={complete ? "text-xs text-success-strong" : "text-xs text-amber-700"}>{complete ? "Pagamento completo" : `Falta ${currency.format(Math.max(0, total - paid))}`}</p></div><button type="button" onClick={() => setPayments((current) => current.length === 1 ? [{ ...current[0], amount: total.toFixed(2) }, { key: crypto.randomUUID(), method: "cash", amount: "0", installments: 1 }] : [...current, { key: crypto.randomUUID(), method: "cash", amount: "0", installments: 1 }])} className="text-xs font-semibold text-brand-deep">+ Adicionar pagamento</button></div>{payments.length > 1 ? <div className="mt-4 space-y-3">{payments.map((payment, index) => <div key={payment.key} className="grid gap-2 rounded-lg bg-background p-3 sm:grid-cols-[1fr_1fr_auto]"><select value={payment.method} onChange={(event) => updatePayment(payment.key, "method", event.target.value)} className="ui-input"><option value="cash">Dinheiro</option><option value="pix">Pix</option><option value="debit_card">Débito</option><option value="credit_card">Crédito</option><option value="on_account">Fiado</option></select><input value={payment.amount} onChange={(event) => updatePayment(payment.key, "amount", event.target.value)} type="number" min="0.01" step="0.01" className="ui-input" aria-label={`Valor do pagamento ${index + 1}`} /><button type="button" onClick={() => setPayments((current) => current.filter((entry) => entry.key !== payment.key))} className="grid size-11 place-items-center text-muted hover:text-red-700" aria-label="Remover pagamento"><Trash2 size={16} /></button></div>)}</div> : null}<button type="button" onClick={() => setPayments((current) => current.map((payment, index) => index === current.length - 1 ? { ...payment, amount: Math.max(0, total - current.slice(0, -1).reduce((sum, item) => sum + (Number(item.amount) || 0), 0)).toFixed(2) } : payment))} className={payments.length > 1 ? "mt-3 text-xs font-semibold text-brand-deep" : "hidden"}>Preencher valor restante</button></div></section> : null}
      {state.message ? <p role="alert" className="rounded-lg bg-red-50 px-4 py-3 text-sm text-red-700">{state.message}</p> : null}<button disabled={pending || !cart.length || total <= 0 || !complete || (hasOnAccount && (!customerId || !dueDate))} className="ui-button-primary w-full disabled:cursor-not-allowed disabled:opacity-45">{pending ? <LoaderCircle className="animate-spin" size={18} /> : <CreditCard size={18} />}{pending ? "Finalizando..." : `Finalizar venda · ${currency.format(total)}`}</button><Link href="/vendas" className="block text-center text-sm font-semibold text-muted hover:text-foreground">Consultar vendas anteriores</Link></div>
  </form>
  {quickCustomerOpen ? <div className="fixed inset-0 z-50 grid place-items-center bg-black/35 p-4" role="dialog" aria-modal="true" aria-labelledby="quick-customer-title"><form action={submitQuickCustomer} className="ui-card w-full max-w-md p-6"><h2 id="quick-customer-title" className="text-lg font-bold">Cadastrar cliente</h2><p className="mt-1 text-sm text-muted">Informe só o nome e o celular para vincular à venda.</p><div className="mt-5 space-y-4"><label><span className="text-sm font-semibold">Nome</span><input name="name" required minLength={2} autoFocus className="ui-input mt-2" placeholder="Nome da cliente" /></label><label><span className="text-sm font-semibold">Celular / WhatsApp</span><input name="phone" required minLength={8} inputMode="tel" className="ui-input mt-2" placeholder="(11) 99999-9999" /></label></div>{quickCustomerError ? <p role="alert" className="mt-4 rounded-lg bg-red-50 px-4 py-3 text-sm text-red-700">{quickCustomerError}</p> : null}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setQuickCustomerOpen(false)} className="ui-button-secondary">Cancelar</button><button disabled={quickCustomerPending} className="ui-button-primary disabled:opacity-50">{quickCustomerPending ? <LoaderCircle className="animate-spin" size={18} /> : null}{quickCustomerPending ? "Salvando..." : "Cadastrar e usar"}</button></div></form></div> : null}
  </>;
}
